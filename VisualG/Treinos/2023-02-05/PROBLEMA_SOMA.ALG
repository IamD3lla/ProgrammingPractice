Algoritmo "problema_soma"

Var

   x, y, soma : inteiro

Inicio

      escreval("Digite valores para x e y formarem uma soma:")
      escreva("Digite o valor de X: ")
      leia(x)
      escreva("Digite o valor de Y: ")
      leia(y)
      
      soma <- x + y
      
      escreva("SOMA = ", soma)

Fimalgoritmo